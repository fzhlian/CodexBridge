{
  "name": "codexbridge-agent",
  "version": "0.1.14",
  "type": "module",
  "main": "dist/extension.js",
  "types": "dist/extension.d.ts",
  "publisher": "fzhlian",
  "repository": {
    "type": "git",
    "url": "https://github.com/fzhlian/CodexBridge.git"
  },
  "license": "SEE LICENSE IN LICENSE.txt",
  "engines": {
    "vscode": "^1.97.0"
  },
  "activationEvents": [
    "onStartupFinished",
    "onCommand:codexbridge.startAgent",
    "onCommand:codexbridge.stopAgent",
    "onCommand:codexbridge.agentStatus"
  ],
  "contributes": {
    "commands": [
      {
        "command": "codexbridge.startAgent",
        "title": "CodexBridge: Start Agent"
      },
      {
        "command": "codexbridge.stopAgent",
        "title": "CodexBridge: Stop Agent"
      },
      {
        "command": "codexbridge.agentStatus",
        "title": "CodexBridge: Agent Status"
      }
    ],
    "configuration": {
      "title": "CodexBridge",
      "properties": {
        "codexbridge.autostart": {
          "type": "boolean",
          "default": false,
          "description": "Automatically start CodexBridge agent when VSCode starts."
        },
        "codexbridge.relayUrl": {
          "type": "string",
          "default": "ws://127.0.0.1:8787/agent",
          "description": "Relay websocket URL."
        },
        "codexbridge.machineId": {
          "type": "string",
          "default": "local-machine",
          "description": "Machine identifier used by CodexBridge relay."
        },
        "codexbridge.reconnectMs": {
          "type": "number",
          "default": 3000,
          "description": "Reconnect interval in milliseconds."
        },
        "codexbridge.heartbeatMs": {
          "type": "number",
          "default": 10000,
          "description": "Heartbeat interval in milliseconds."
        },
        "codexbridge.pendingTimeoutMs": {
          "type": "number",
          "default": 300000,
          "description": "Drop queued user messages when waiting longer than this timeout (milliseconds)."
        },
        "codexbridge.cloudflaredCheckMs": {
          "type": "number",
          "default": 15000,
          "description": "Cloudflared health-check interval in milliseconds. Agent auto-restarts cloudflared when it exits."
        },
        "codexbridge.contextMaxFileChars": {
          "type": "number",
          "default": 12000,
          "description": "Maximum characters sent for active file context."
        },
        "codexbridge.contextMaxSelectionChars": {
          "type": "number",
          "default": 6000,
          "description": "Maximum characters sent for selected text context."
        }
      }
    }
  },
  "categories": [
    "Other"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint src test --ext .ts",
    "test": "vitest run",
    "dev": "pnpm run build && node dist/src/index.js",
    "dev:node": "pnpm run build && node dist/src/index.js",
    "package:vsix": "node ./scripts/package-vsix.mjs"
  },
  "dependencies": {
    "@codexbridge/codex-client": "workspace:*",
    "@codexbridge/shared": "workspace:*",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "@types/ws": "^8.18.1",
    "@vscode/vsce": "^3.2.2",
    "@types/vscode": "^1.97.0"
  }
}
